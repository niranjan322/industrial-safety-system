{
  "name": "industrial-safety-system",
  "version": "1.0.0",
  "description": "Root package.json to deploy both backend and frontend on a single Render Web Service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
